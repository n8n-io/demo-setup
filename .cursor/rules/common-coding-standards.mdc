---
description: "Common — Naming, repo structure, commit format"
alwaysApply: true
---

# Common — Coding Standards

## Repository Standards

### Required Files

README.md, CONTRIBUTING.md, ARCHITECTURE.md, SECURITY.md, RUNBOOK.md, .env.example, Makefile or taskfile.yml, docker-compose.yml (if applicable).

### Recommended Structure

`/n8n/workflows/`, `/n8n/credentials/`, `/tests/`, `/docs/`, `/scripts/`

## Naming Conventions

- **n8n workflows:** kebab-case filenames (`workflow-name.json`)
- **Docker services:** kebab-case names in docker-compose.yml
- **Config files:** kebab-case (YAML, JSON, etc.)
- **JSON keys:** camelCase or kebab-case where applicable
- **Services:** Pattern `<domain>-<service>`

## Commit Message Standard

```
<type>: <short description>
```

Types: feat, fix, refactor, test, chore, docs, security.
